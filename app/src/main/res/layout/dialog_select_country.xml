<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="10dp">

    <TextView
        android:id="@+id/selectCountry_LBL_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:text="Select Origin Country"
        android:textColor="@color/colorPrimaryDark"
        android:textSize="25sp"
        android:textStyle="bold" />


    <androidx.appcompat.widget.AppCompatSpinner
        android:id="@+id/selectCountry_LST_countrySpinner"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_below="@id/selectCountry_LBL_title"
        android:layout_alignStart="@id/selectCountry_LBL_title"
        android:layout_alignEnd="@id/selectCountry_LBL_title"
        android:layout_marginTop="10dp"
        android:background="@drawable/rectangular_spinner_border" />

    <TextView
        android:id="@+id/selectCountry_LBL_confirm"
        android:layout_width="match_parent"
        android:layout_height="35dp"
        android:layout_below="@id/selectCountry_LST_countrySpinner"
        android:layout_alignStart="@id/selectCountry_LST_countrySpinner"
        android:layout_alignEnd="@id/selectCountry_LST_countrySpinner"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp"
        android:background="@drawable/round_login_button_backgroud"
        android:clickable="true"
        android:foreground="?attr/selectableItemBackground"
        android:gravity="center"
        android:text="SUBMIT"
        android:textColor="@color/white"
        android:textSize="20sp"
        android:textStyle="bold" />

</RelativeLayout>